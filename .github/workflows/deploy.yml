name: Example Action

on:
  push:
    branches:
      - prod

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run test script
        run: echo "✅ GitHub Actions is working!"
