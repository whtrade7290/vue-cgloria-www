name: Test Action

on:
  push:
    branches:
      - prod  # 혹은 prod 등, 지금 사용하는 브랜치명으로 변경

jobs:
  test-run:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Step 1 - Checkout repository
        uses: actions/checkout@v4

      - name: 🧩 Step 2 - Run a test command
        run: echo "🎉 GitHub Actions is working!"
